services:
  my-service:
    container_name: my-service
    hostname: my-service
    environment:
      TZ: Europe/Istanbul
    volumes:
      - ../../_data:/data
    network_mode: nas
    restart: unless-stopped
    image: my-service:latest
